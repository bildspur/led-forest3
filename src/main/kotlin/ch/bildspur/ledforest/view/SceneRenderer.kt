package ch.bildspur.ledforest.view

import ch.bildspur.ledforest.model.light.Tube
import processing.core.PGraphics

class SceneRenderer(val g: PGraphics, val tubes: List<Tube>) {

    /*
    fun initShapes(ledLength: Float, rodWidth: Float, rodDetail: Int) {
        shapes = mutableListOf()
        val rodLength = ledLength * leds.size

        for (i in 0..leds.size - 1) {
            val sh = g.createRod(rodWidth, ledLength, rodDetail)
            sh.disableStyle()
            shapes.add(sh)
        }
    }
    */
}